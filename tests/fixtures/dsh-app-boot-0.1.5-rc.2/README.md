# Published patch fixture

`lib/index.js` is the unmodified published `@deepseek-ai/dsh-app-boot@0.1.5-rc.2` entrypoint, extracted from the official npm tarball (`https://registry.npmjs.org/@deepseek-ai/dsh-app-boot/-/dsh-app-boot-0.1.5-rc.2.tgz`). MIT license is included. It tests patch compatibility against real upstream code, including the rc2 canonical symlink comparison.
