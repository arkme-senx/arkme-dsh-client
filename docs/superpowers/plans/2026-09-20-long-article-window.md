# 独立长文窗口实现计划

用户已确认原型并要求沿用现有 App 风格，本次直接执行，不提交或发布。

Electron 主进程创建专用非模态窗口，共用 Harness 来源和 session。插件单独挂载现有编辑器，保留富文本和图片能力。打开时固定账号、会话和显示名称。回执只刷新绑定目标。

- [x] 窗口管理：去重、来源校验、账号失效、关闭和退出保护。
- [x] 编辑器：独立布局、保存状态、关闭三选项、发送重试与账号保护。
- [x] 入口与回执：新建长文接入独立窗口，已有长文选择保持现状。
- [x] 验证：相关测试、类型检查、构建、Electron 冒烟及视觉检查。

重点验证切换会话不串发、切换账号失效、保存失败不关闭、退出前保存、发送重试幂等。

验证结果与复跑方式见 ../../long-article-window.md。独立审查发现失效账号关闭绕过草稿保护，已补显式放弃与回归测试。
